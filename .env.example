# Groww API credentials for historical-data backfill.
# Copy this file to .env (which is gitignored) and fill in your values.
# Never commit the actual .env file. Rotate credentials immediately if exposed.

GROWW_API_KEY=replace_me
GROWW_API_SECRET=replace_me
