# ⚡ QUICK COMMAND CARD - STOCK ANALYSIS v2.0.0
# ===============================================

## 🚀 INSTANT COMMANDS (Copy & Paste)

### **🔥 MOST USED COMMANDS**
```bash
# 1. Interactive Mode (Guided)
python main.py --interactive

# 2. Quick Test (5 stocks, 30 seconds)
python main.py --risk-profile aggressive -n 5

# 3. Single Stock Analysis
python main.py -s RELIANCE --risk-profile aggressive

# 4. Growth Focus (Best for investing)
python main.py --risk-profile aggressive --focus-growth -n 10

# 5. Momentum Focus (Best for trading)
python main.py --risk-profile aggressive --focus-momentum -n 10

# 6. Ultimate Combo (Growth + Momentum)
python main.py --risk-profile aggressive --focus-growth --focus-momentum -n 15

# 7. Portfolio Builder (₹5 Lakhs)
python main.py --portfolio-amount 500000 --risk-profile aggressive --focus-growth

# 8. High Volatility Hunter (≥20%)
python main.py --risk-profile aggressive --min-volatility 20 -n 20

# 9. Tools Menu
python main.py --tools

# 10. Help & Options
python main.py
```

## ⚡ **POPULAR STOCK SYMBOLS**
```bash
# Large Cap Winners
python main.py -s RELIANCE --risk-profile aggressive
python main.py -s TCS --risk-profile aggressive
python main.py -s INFY --risk-profile aggressive
python main.py -s BHARTIARTL --risk-profile aggressive

# High Growth Picks
python main.py -s TATAMOTORS --risk-profile aggressive --focus-growth
python main.py -s HINDALCO --risk-profile aggressive --focus-growth
python main.py -s ADANIPORTS --risk-profile aggressive --focus-growth

# Momentum Plays
python main.py -s WIPRO --risk-profile aggressive --focus-momentum
python main.py -s TECHM --risk-profile aggressive --focus-momentum
python main.py -s HCLTECH --risk-profile aggressive --focus-momentum
```

## 🎯 **BY TIME AVAILABLE**

### **⚡ 30 Seconds - Quick Peek**
```bash
python main.py -s RELIANCE --risk-profile aggressive
python main.py --risk-profile aggressive -n 5
```

### **🚀 2 Minutes - Fast Scan**
```bash
python main.py --risk-profile aggressive --focus-growth -n 10
python main.py --risk-profile aggressive --focus-momentum -n 10
```

### **📈 5 Minutes - Good Analysis**
```bash
python main.py --risk-profile aggressive --focus-growth --focus-momentum -n 15
python main.py --risk-profile aggressive --min-volatility 15 -n 20
```

### **💰 15 Minutes - Full Power**
```bash
python main.py --risk-profile aggressive --focus-growth --focus-momentum
python main.py --portfolio-amount 500000 --risk-profile aggressive
```

## 🔥 **BY INVESTMENT STYLE**

### **📊 Day Trader**
```bash
python main.py --risk-profile aggressive --focus-momentum --min-volatility 25 -n 15
```

### **📈 Swing Trader**
```bash
python main.py --risk-profile aggressive --focus-growth --focus-momentum -n 25
```

### **💰 Long-term Investor**
```bash
python main.py --risk-profile aggressive --focus-growth --portfolio-amount 500000
```

### **🎪 Opportunity Hunter**
```bash
python main.py --risk-profile aggressive --min-volatility 20 --focus-momentum -n 30
```

## 🛠️ **TOOLS & UTILITIES**
```bash
# Interactive command builder
python tools/command_builder.py

# Setup verification
python tools/verify_setup.py

# Investor guide
python tools/investor_guide.py

# Project organization
python organize_project.py
```

## 💾 **OUTPUT LOCATIONS**
```bash
# Excel Reports: data/analysis_results/
# Logs: logs/
# CSV Exports: data/exports/
# Backup Reports: reports/
```

## 🆘 **QUICK HELP**
```bash
# Main help
python main.py

# Quick help
python main.py --help-quick

# Tools menu
python main.py --tools

# Interactive mode
python main.py --interactive
```

---

**🎯 PICK ANY COMMAND & START ANALYZING! 📈🚀**

*Pro Tip: Start with `python main.py --interactive` for guided experience!*
